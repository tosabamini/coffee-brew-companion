# Coffee Brew Companion — アプリ全体解説資料

> この文書は「授業でAIをどう活用し、どんなアプリをAIと一緒に作ったか」を説明する**プレゼン資料を別のAIに作ってもらうための元ネタ**です。
> 初学者にも分かるよう、専門用語には逐一かみ砕いた説明を添えています。情報量はあえて多めにしてあります。プレゼンに使う部分だけ取捨選択してください。

---

## 0. この資料の読み方・前提

- 本アプリは **2つのソフトウェアがペアで動く「ハードウェア連携アプリ」** です。
  1. **スマホアプリ（Flutter製）** … ユーザーが触る画面。
  2. **自作の電子はかり（ESP32マイコン製）** … コーヒーの重さを測ってスマホに無線で送る装置。
- 「いつ追加されたか」について：このプロジェクトは**Gitのコミット履歴が残っていない**（まだ一度もコミットされていない状態）ため、正確な日付は取得できません。
  本資料の「開発の経緯・順序」は、**コードの中に残された痕跡（古い形式と新しい形式が共存しているコード、後方互換のための処理、未使用の置き場フォルダなど）から推定した“開発ストーリー”** です。事実（コードに存在する機能）と推定（追加順）は section ごとに区別して書いています。

---

## 1. アプリは何をするものか（ひとことで）

> **「ハンドドリップでコーヒーを淹れるとき、お湯を注ぐ重さと時間をリアルタイムでグラフ表示し、レシピ通りに注げているかを助け、淹れた記録を残す」アプリ。**

コーヒーを美味しく淹れるには「何秒の時点で何グラムのお湯を注ぐか」が重要です。本アプリは：

1. 自作のはかりにドリッパーを乗せる
2. お湯を注ぐと、重さの増え方が**折れ線グラフ**としてリアルタイムに描かれる
3. あらかじめ登録した**レシピ（目標の注湯タイミングと量）**が点線でグラフに重なって表示される
4. 「今ちょうどレシピ通り注げている／遅れている」が一目で分かる
5. 淹れ終わったら豆の情報・味の記録と一緒にセッションを保存できる

さらに、コーヒー豆の袋（パッケージ）を**写真に撮るだけで、AI（Claude）が産地・品種・焙煎度などを自動で読み取って入力欄を埋めてくれる**機能があります。ここが「AIをアプリに組み込んだ」ポイントです。

---

## 2. システム全体像（2つのソフトの関係）

```
┌─────────────────────────────┐         BLE（Bluetooth Low Energy）          ┌──────────────────────────────┐
│  自作はかり（ESP32マイコン）      │  ── 約150〜210msごとに重さを無線送信 ──▶   │   スマホアプリ（Flutter）         │
│                              │                                              │                              │
│  ・HX711(ロードセル増幅器)で     │   送るデータ:  "12.3"（重さの文字列）         │  ・グラフ描画                   │
│    重さを読む                  │                                              │  ・レシピ照合                   │
│  ・ノイズ除去フィルタ            │   ◀── 接続/切断（スマホ側から要求）──        │  ・記録の保存                   │
│  ・BLEで重さを配信              │                                              │  ・AIで豆情報を読み取り（ネット経由）│
└─────────────────────────────┘                                              └──────────────────────────────┘
                                                                                          │
                                                                                          │ HTTPS（インターネット）
                                                                                          ▼
                                                                              ┌──────────────────────────┐
                                                                              │  Claude API（Anthropic社）  │
                                                                              │  画像→豆情報の抽出           │
                                                                              └──────────────────────────┘
```

**ポイント：通信は2種類ある**
- **BLE（近距離無線）**：はかり ↔ スマホ。インターネット不要。重さデータが流れる。
- **HTTPS（インターネット通信）**：スマホ ↔ Claude API。豆パッケージ写真を解析するときだけ使う。

---

## 3. 用語ミニ辞典（初学者向け）

| 用語 | かみ砕いた意味 |
|------|----------------|
| **Flutter（フラッター）** | Googleが作った、スマホアプリを作る道具。1つのコードでAndroidもiOSも動かせる。プログラミング言語は **Dart** を使う。 |
| **ESP32（イーエスピー32）** | 小さくて安いマイコン（超小型コンピュータ）。Wi‑FiとBluetoothを内蔵していて、電子工作の定番。 |
| **ロードセル / HX711** | ロードセルは「重さを電気信号に変える部品」。HX711はその弱い信号を増幅してマイコンが読めるようにする部品。合わせて「電子はかりの心臓部」。 |
| **BLE** | Bluetooth Low Energy。低消費電力の近距離無線。イヤホンや体重計でよく使われる。 |
| **GATT / Characteristic（キャラクタリスティック）** | BLEでデータをやり取りする時の“住所と引き出し”のような仕組み。「この引き出し（Characteristic）に重さを入れておくよ」とマイコンが用意し、スマホがそこを覗く。 |
| **Notify（通知）** | BLEで「値が変わったら自動でスマホに教える」仕組み。スマホが何度も聞きに行かなくていいので効率的。 |
| **Stream（ストリーム）** | Dartの「データが流れ続ける川」のような仕組み。重さが次々流れてくるのをUIが受け取る。 |
| **CustomPainter** | Flutterで「自分でゼロから図形を描く」ための道具。本アプリのグラフはグラフ用ライブラリを使わず、これで線を1本ずつ描いている。 |
| **JSON** | データを文字で保存する形式。アプリは記録をこの形式でスマホ内に保存する。 |
| **Vision API（ビジョンAPI）** | 画像を見て内容を理解できるAI機能。ここではClaudeに豆袋の写真を渡し、文字情報を読み取らせている。 |

---

## 4. スマホアプリの機能一覧（画面ごと）

アプリは「画面（Screen）」の集まりです。各画面の役割を説明します。

### 4-1. メイン画面（`main_screen.dart`）
アプリを開いて最初に出る、計測の中心となる画面。

**できること：**
- **Bluetooth接続ボタン**：はかりを探して接続／切断する。接続状態は画面上部の丸い色（緑＝接続、赤＝未接続）と文字で分かる。
- **現在の重さ表示**：はかりから来る重さを大きな数字で表示。
- **Tare（風袋＝ふうたい）ボタン**：今の重さを「0」とみなすボタン。ドリッパーやサーバーを乗せた状態で押すと、その分を引いてお湯だけの重さを測れる。（料理用はかりの「0表示」と同じ）
- **Start／Stop（計測開始・停止）**：押すと、その瞬間からの「経過時間」と「重さ」を記録し始め、折れ線グラフがリアルタイムで伸びていく。Stopで保存画面へ進む。
- **経過時間タイマー**：計測中の時間を `分:秒` で表示（重さデータとは独立して1秒ごとに動く）。
- **レシピ選択チップ**：登録済みレシピを横スクロールで切り替え。「なし」も選べる。
- **豆量(Bean Quantity)の増減**：選択中レシピの想定豆量を ± ボタンで微調整でき、レシピの目標量が自動で比例計算されて変わる（後述のスケーリング）。
- **豆量計測ボタン**：「タレ（0化）→ 豆を乗せる → 決定」で、実際に使う豆の量を測ってレシピに反映する。
- **グラフ表示**：画面下半分に、重さ×時間のグラフ＋レシピの目標ライン（点線）を重ねて表示。
- 上部メニューから他画面（豆情報、フレーバーホイール、レシピ設定、保存済み記録）へ移動できる。

### 4-2. レシピ設定画面（`recipe_settings_screen.dart`）
「何秒で何グラムまで注ぐ」という淹れ方の計画を作る画面。

- レシピ名、想定豆量、最大6段階の「注湯ステップ」を登録できる。
- ステップの呼び名は **Blooming（蒸らし）/ 2nd Pour / 3rd Pour …** と固定ラベル。
- 1段目（Blooming）は開始時刻が必ず `0:00` に固定される。
- 各ステップは「開始時刻（MM:SS）」と「その時点での累計目標重量(g)」で表す。
- 目標終了時刻（Target End Time）も任意で設定可能。
- 複数のレシピを保存・編集・削除でき、選択中のレシピは次回起動時も覚えている。
- 入力チェック（時刻は MM:SS 形式、重さは数値、など）があり、不正なら警告を出す。

### 4-3. 保存画面 / 豆情報画面（`save_session_screen.dart`）
計測を止めた後、または上部メニューから単独で開く、記録入力画面。

**記録できる豆の情報：**
- 豆の名前、産地国（ドロップダウン＋自由入力）、地域/農園、品種、精製方法（Natural/Washed/Honeyなど）、焙煎度、挽き目、フレーバーノート（複数選択）、標高、メモ。
- 産地・品種・精製方法は**よく使う選択肢があらかじめ用意**されていて、無ければ「Other」で自由入力できる。

**便利機能：**
- **「パッケージ写真から自動入力」ボタン（AI機能）**：豆袋をカメラ撮影 or ライブラリから選ぶと、Claude AIが画像を解析して上記の入力欄を自動で埋める。
- **保存済み豆情報の再利用**：過去に入力した豆情報を選んでワンタップで再入力できる（同じ豆を繰り返し淹れる人向け）。
- 計測データ（グラフ）があるときはグラフも一緒に表示・保存。単独で開いたとき（Bean Info）はグラフなしで豆情報だけ登録できる。

### 4-4. 保存済みセッション一覧（`history_screen.dart`）
過去に保存した記録の一覧。豆名・日時・焙煎度・最大重量が並ぶ。タップで詳細へ、ゴミ箱で削除。

### 4-5. セッション詳細画面（`session_detail_screen.dart`）
1件の記録の詳細表示・編集。

- グラフ＋全項目を表示し、後から豆情報を編集して上書き保存できる。
- **画像エクスポート機能**：記録カード（豆情報＋レシピ＋グラフ）を**1枚のPNG画像として書き出す**。SNS共有や記録の見返しに使える。（画面外に高解像度のカードを隠し描きして、それを画像化する仕組み）

### 4-6. フレーバーホイール画面（`flavor_wheel_screen.dart`）
コーヒーの味を専門的に表す「**SCAフレーバーホイール**」（コーヒー業界標準の味の分類円グラフ）を、自分で描画・カスタマイズできる画面。

- **CustomPainterで円形のホイールを自前描画**（`flavor_wheel_painter.dart`）。
- 回転させたり、拡大表示で内側（大分類）→外側（小分類）を見たりできる。
- デフォルトで Fruity / Floral / Sweet / Nutty‑Cocoa / Spices / Roasted / Green‑Vegetative / Sour‑Fermented / Other などの分類が入っている。
- ユーザーが独自のフレーバー語を追加・分類でき、その内容は豆情報入力の「フレーバーノート選択」と連動する。

> ※ コードコメント上、この機能は「**部分的に実装済み**」の位置づけ（CLAUDE.md に partially implemented と記載）。プレゼンでは「発展機能・実験的機能」として扱うと正確です。

---

## 5. データの流れ（技術的な詳細）

```
ESP32（HX711で重さを読む → BLEで約210msごとにNotify送信）
  → BleScaleService（スマホ側のBLE担当。重さ・接続・状態を「Stream」で配信）
  → MainScreen（Streamを受け取り、計測中なら時刻つきで点を記録）
  → WeightGraph（受け取った点をCustomPainterで折れ線に描画）
  → SaveSessionScreen（豆情報を足して）
  → CoffeeSession（1回分の記録オブジェクト）
  → SessionStorage（スマホ内にJSONファイルとして保存）
```

### 主要なデータの“かたち”（モデル）

| モデル名 | 何を表すか | 主な中身 |
|----------|-----------|----------|
| `WeightPoint` | グラフの点1つ | 経過ミリ秒 `elapsedMs`、重さ `weightG` |
| `PourStep` | レシピの注湯1段階 | 段番号、開始秒、累計目標g |
| `BrewRecipe` | 淹れ方レシピ全体 | ID、名前、想定豆量、目標終了秒、PourStepの配列 |
| `CoffeeSession` | 1回の抽出の記録全体 | ID、日時、豆情報一式、レシピのコピー、WeightPointの配列、フレーバーノート |
| `FlavorNoteTag` | 味タグ1つ | 名前、分類名、色、ユーザー入力かどうか |
| `FlavorCategory` 他 | フレーバーホイールの分類 | 大分類→中分類→小分類の3階層構造 |

### 状態管理（少し技術寄り）
本アプリは Provider / Riverpod などの外部状態管理ライブラリを**あえて使わず**、Flutter標準の仕組みだけで作られています。
- BLEのデータは `StreamController`（川の発生源）で配信。
- 画面の状態は `setState`（画面を描き直す命令）で更新。
- BLEの川を `StreamBuilder` / `listen` で受け取る。
- 経過時間は `Timer.periodic`（1秒ごとに動くタイマー）で別管理。

→ **学習・プレゼン上の意味**：「外部ライブラリに頼らず、フレームワークの基本機能だけでリアルタイムUIを実現している」＝シンプルで仕組みが追いやすい設計。

### データの保存場所
- 記録やレシピは**スマホ本体の中（アプリ専用フォルダ）にJSONファイルとして保存**。クラウドではなくローカル保存。
  - `coffee_sessions.json`（記録一覧）
  - `brew_recipes.json`（複数レシピ）
  - `current_recipe_id.txt`（選択中レシピのID）
  - `flavor_wheel.json` / `flavor_uncategorized.json`（フレーバー情報）

---

## 6. デバイス（はかり）との接続：BLEの詳細

プレゼンで「どんなふうにデバイスとつないでいるか」を語る用の詳細です。

### 6-1. BLEプロファイル（合言葉のようなもの）
はかりとスマホは、決まったIDで“お互いを見つけて”通信します。

| 項目 | 値 | 説明 |
|------|----|------|
| デバイス名 | `CoffeeScale` | はかりが名乗る名前 |
| Service UUID | `12345678-1234-1234-1234-1234567890ab` | サービス（機能のまとまり）の識別子 |
| Characteristic UUID | `abcdefab-1234-1234-1234-abcdefabcdef` | 重さデータが入る“引き出し”の識別子 |
| データ形式 | UTF‑8文字列（例 `"12.3"`） | 数字を文字として送る。スマホ側で数値に変換 |
| 送信間隔 | 約150〜210ms | はかりが繰り返し重さを送る |

### 6-2. 接続の流れ（スマホ側 `ble_scale_service.dart`）
1. **権限の確認**（Androidのみ）：Bluetoothスキャン・接続・位置情報の許可を求める（Androidは仕様上、BLEスキャンに位置情報許可が必要）。
2. **スキャン（探す）**：周囲のBLE機器を最大8秒間探し、「名前が CoffeeScale」または「目的のService UUIDを広告している」機器を見つける。
3. **接続**：見つけた機器に接続。
4. **サービス探索**：目的のService/Characteristicを探し当てる。
5. **Notify購読**：「重さが更新されたら教えて」と登録。以後、重さが来るたびにアプリ内の`weightStream`に流す。
6. **切断処理**：購読解除・接続解除をきれいに行い、画面に状態を反映。

**工夫ポイント（プレゼン映えする技術的こだわり）：**
- `onValueReceived` を使い、`lastValueStream`（再接続時に古い値が"再生"される）を避けている → **再接続直後に古い重さが出ない**。
- 接続状態・スキャン状況・重さを**3本の別々のStream**に分けて配信 → UIが状態を分かりやすく扱える。

### 6-3. はかり側（ESP32ファーム `coffee_scale_ble.ino`）
C++（Arduino言語）で書かれた、はかりの中身のプログラム。

**重さを正確にするための工夫：**
- **DT=4, SCK=5** ピンでHX711と接続。
- **ゼロ点校正**：起動時に何も乗っていない状態の値を10回平均して「0の基準（offset）」を決める。
- **トリム平均フィルタ**：1回の測定で5サンプル取り、**最小値と最大値を捨てて中間3つを平均**。突発的なノイズ（スパイク）を除去。
- **`wait_ready()`** で変換完了を待ってから読むので、同じ値を二重に読まない。
- **スケーリング係数 `236.2`**：生の数値を「グラム」に変換する、このハード固有の定数。
- 重さ = （生値 − offset）÷ 236.2 で算出。

**BLE側の工夫：**
- 接続が切れたら **300ms待ってから**広告（advertising）を再開（即再開だと不安定なため）。
- `setMinPreferred(0x06)` / `(0x12)` という、**iPhone/Android双方で安定しやすい定番の接続間隔設定**を入れている。

---

## 7. AI活用ポイント（授業の主題）

この資料の核。**「AIをどう使ったか」は2つの層**があります。

### 7-1. 【アプリの“機能”としてのAI】豆パッケージ自動読み取り
- 実装：`claude_vision_service.dart`
- 使用モデル：**Claude Haiku（`claude-haiku-4-5`）** — 速くて安価な画像対応モデル。
- 仕組み：
  1. ユーザーが豆袋を撮影／選択。
  2. 画像をBase64という文字列に変換し、Claude APIへHTTPSで送信。
  3. プロンプト（指示文）で「豆名・産地国・地域/農園・品種・精製方法・焙煎度・標高・フレーバーノート・備考を、**JSON形式だけで**返して」と指定。
  4. 返ってきたJSONを解析し、入力フォームに自動反映。ユーザーは確認・修正するだけ。
- 価値：**手入力が面倒な豆情報を、写真1枚で9割方埋められる**。AIの「画像理解＋構造化出力」を実用機能に落とし込んだ好例。

### 7-2. 【開発の“相棒”としてのAI】コードをAIと一緒に書いた
- このプロジェクトには `CLAUDE.md` という、**AI（Claude Code）への作業指示書**が置かれている。
  - 「回答は日本語で」「変更前に原因と方針を説明」「変更は最小限」「FlutterのUI変更は対象Widgetと状態の流れを先に確認」といった、AIに開発を手伝わせるためのルールが書かれている。
- つまり本アプリは、**AIをペアプログラマー（一緒にコードを書く相棒）として開発した**プロジェクト。
- プレゼンでは「①完成アプリの中でAIが働く（Vision機能）」「②開発中にAIが働く（コーディング支援）」の**二重のAI活用**として語ると強い。

> ⚠️ 注意：`api_config.dart` にAPIキーを直接書く方式になっています。本番・公開時は危険なので、プレゼンでは「鍵は本来 .gitignore で除外して秘匿する設計」と説明し、実際の鍵は資料に載せないでください。

---

## 8. 開発の経緯・追加順（コードからの推定）

> **重要**：Git履歴が無いため日付は不明。以下は**コード内の痕跡から推定した“おそらくこの順で育った”ストーリー**です。プレゼンでは「推定」と添えるのが誠実です。

痕跡となった証拠：
- `CoffeeSession` に `flavorNote`（旧・単一文字列）と `flavorNotes`（新・複数タグ）が**両方ある**＋コメントに「新形式／旧形式」と明記 → 後から複数タグ対応に拡張した。
- `SessionStorage` に単一レシピ用 `current_brew_recipe.json` の処理と、複数レシピ用 `brew_recipes.json` の処理が**両方ある** → 単一レシピ→複数レシピへ進化した。
- `lib/features/` 配下に `bean_notes/ grind_analysis/ recipe/ shared/` という**中身の薄い置き場フォルダ**がある → 将来拡張を見越した足場（CLAUDE.md にも「scaffolded placeholders」と記載）。
- `flavor_wheel/` はコメント上「partially implemented（部分実装）」。

**推定タイムライン（段階）：**

| 段階 | 内容 | 推定の根拠 |
|------|------|-----------|
| ① 土台 | ESP32はかり＋BLEで重さをスマホに送り、現在重量を表示 | 最も基礎的でこれが無いと何も動かない |
| ② 計測とグラフ | Start/Stopで時系列記録、CustomPainterでリアルタイムグラフ化 | `WeightPoint`/`WeightGraph` が中核 |
| ③ 記録の保存 | セッションを豆情報つきでJSON保存、一覧・詳細・編集 | `SessionStorage`/`history`/`detail` |
| ④ レシピ（単一） | 注湯ステップ目標をグラフに点線オーバーレイ | `current_brew_recipe.json` の痕跡 |
| ⑤ レシピ（複数化） | 複数レシピの保存・切替・選択記憶 | `brew_recipes.json` + `current_recipe_id` の追加 |
| ⑥ 豆量スケーリング | 実際の豆量に合わせ目標量を比例自動計算 | `scaledForBeanQuantity()` と豆量計測UI |
| ⑦ フレーバーノート拡張 | 単一文字列→複数タグ＋色分類へ | 旧`flavorNote`/新`flavorNotes`の共存 |
| ⑧ フレーバーホイール | SCA準拠の味分類を自前描画・カスタム | `flavor_wheel/`（部分実装） |
| ⑨ AI画像読み取り | Claude Visionで豆袋を自動入力 | `claude_vision_service.dart` + http依存 |
| ⑩ 画像エクスポート | 記録を1枚のPNGに書き出し | `session_detail_screen` のRepaintBoundary処理 |

（①〜③が基礎、④〜⑥がレシピ系の強化、⑦〜⑩が「使いやすさ・付加価値」系の拡張、という3波で育ったと読めます。）

---

## 9. 技術スタック early summary（プレゼン用早見表）

| 区分 | 使っているもの |
|------|----------------|
| スマホアプリ | Flutter / Dart |
| 主要パッケージ | `flutter_blue_plus`（BLE）, `permission_handler`（権限）, `path_provider`（保存先取得）, `image_picker`（カメラ/写真）, `http`（API通信） |
| グラフ描画 | 外部ライブラリ不使用。`CustomPainter`で自前実装 |
| 状態管理 | 外部ライブラリ不使用。`StreamController`+`setState`+`Timer` |
| データ保存 | ローカルJSON（クラウド不使用） |
| ハードウェア | ESP32マイコン + HX711 + ロードセル |
| ファーム言語 | C++（Arduino） |
| 無線 | BLE（GATT Notify） |
| AI連携 | Claude API（Vision：`claude-haiku-4-5`）をHTTPSで呼び出し |
| 開発支援AI | Claude Code（`CLAUDE.md`で作業ルールを規定） |

---

## 10. このアプリの“推せる”ポイント（プレゼンの主張候補）

1. **ハード × ソフト × AI の三位一体**：自作電子工作（はかり）、スマホアプリ、生成AIを1つの体験に統合している。
2. **リアルタイム性**：BLE通知＋自前グラフ描画で、注湯の様子が遅延少なく可視化される。
3. **AIの実用的な使い方**：「画像→構造化データ」というAIの強みを、面倒な手入力の自動化という具体的価値に変えている。
4. **AIとの協働開発**：`CLAUDE.md`によりAIを“ルールを守る開発パートナー”として運用した。
5. **堅実な設計判断**：外部状態管理ライブラリを使わず標準機能で完結。ノイズ除去や再接続の安定化など、現場で効く地味な工夫が随所にある。
6. **記録文化**：淹れた結果を画像で残し振り返れる＝学び・改善のループを支える。

---

## 11. 注意点・限界（正直に書いておくと資料の信頼性が上がる）

- **APIキーの扱い**：現状はソースに直書き。公開・配布には不向き（要・環境変数や安全な保管へ）。**この資料を共有する前に既存キーは必ず無効化を。**
- **フレーバーホイールは部分実装**：完成機能として強調しすぎない。
- **置き場フォルダ（bean_notes 等）は中身がほぼ空**：構想止まりの部分がある。
- **保存はローカルのみ**：機種変更やバックアップ無しでのアプリ削除でデータは消える（クラウド同期は未実装）。
- **Git履歴なし**：開発順は推定。発表で「いつ」を断定しないこと。

---

## 12. 付録：ファイル早見表

| ファイル | 役割 |
|----------|------|
| `coffee_scale_ble/coffee_scale_ble.ino` | はかり（ESP32）のプログラム。重さ計測＋BLE配信 |
| `lib/main.dart` | アプリの起動点。テーマ（茶色基調）設定 |
| `lib/features/scale/services/ble_scale_service.dart` | BLEのスキャン・接続・重さ受信（Stream配信） |
| `lib/features/scale/services/session_storage.dart` | 記録・レシピのJSON保存/読込 |
| `lib/features/scale/services/claude_vision_service.dart` | Claude Visionで豆袋写真を解析 |
| `lib/features/scale/screens/main_screen.dart` | 中心画面。計測・接続・レシピ選択・豆量計測 |
| `lib/features/scale/screens/recipe_settings_screen.dart` | レシピ作成・編集・削除 |
| `lib/features/scale/screens/save_session_screen.dart` | 豆情報入力・AI自動入力・保存 |
| `lib/features/scale/screens/history_screen.dart` | 保存記録の一覧 |
| `lib/features/scale/screens/session_detail_screen.dart` | 記録の詳細・編集・PNGエクスポート |
| `lib/features/scale/widgets/weight_graph.dart` | 重さ×時間グラフの自前描画（レシピ点線重ね） |
| `lib/features/scale/models/*.dart` | データのかたち（WeightPoint/PourStep/BrewRecipe/CoffeeSession） |
| `lib/features/flavor_wheel/**` | SCAフレーバーホイールの描画・編集（部分実装） |
| `lib/features/shared/**` | フレーバータグの共有モデル・選択UI |
| `lib/config/api_config.dart` | APIキー設定（本来は秘匿対象） |
| `CLAUDE.md` | 開発支援AI（Claude Code）への作業ルール |

---

*（この資料は実際のソースコードを読み取って作成。機能=コードに存在する事実、開発順=コードの痕跡からの推定、という区別で記述しています。）*
